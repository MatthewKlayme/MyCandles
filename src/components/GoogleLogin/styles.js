import { StyleSheet } from "react-native";
import {colors} from "../../utils/colors";

export const styles = StyleSheet.create({
    btnStyle: {
        backgroundColor: colors.darkgrey,
        alignItems: "center", //aligns on x axis
        justifyContent: "center", //aligns on y axis
        alignSelf: "center",
        paddingVertical: 16,
        width: "45%",
        borderRadius: 14
    },

    google: {
        
    },
})